/** App routes. Detail routes carry a placeholder replaced at navigation time. */
export enum Routs {
  HOME = "/",
  WORK = "/work",
  WORK_DETAILS = "/work/SLUG",
  ABOUT = "/about",
  CONTACT = "/contact",
}

/** API endpoints (internal Route Handlers). */
export enum Endpoints {
  CONTACT = "/api/contact",
}
